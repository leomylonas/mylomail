!include Base.conf

# Tier 1 — everything. Capabilities are left at Dovecot's defaults, which advertise
# CONDSTORE, QRESYNC, UIDPLUS and MOVE. New mail by increasing UID, flag changes by
# mod-sequence, expunges via VANISHED (§3).
namespace inbox {
  separator = /
}
